# 常用命令记录

### 1. 还原某个修改的文件
```bash
git restore file
``` 

### 2. 查看当前分支名
`git branch `

`git checkout -b`

`git log`

`git status`
--short

`git pull`

`git push`

`git merge`

`git revert`

`git add`

`git commit`

### 顺序可以按照开发的顺序进行
